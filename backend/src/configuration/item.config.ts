import { ItemRarity } from "src/api/items/item/model/definitions/item-rarity";

export const DEFAULT_LEVEL = 0;
export const DEFAULT_RARITY = ItemRarity.Common;
export const DEFAULT_QUANTITY = 1;
export const DEFAULT_POSITION = 0;

export const DEFAULT_STARTING_FOOD_QUANTITY = 10;

export const USER_RELATION = ['user']

export const MIN_AUCTION_TIME = 1;  // 1 hour
export const MAX_AUCTION_TIME = 72;  // 72 hours
